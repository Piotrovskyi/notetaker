# Notetaker

Run
```sh
python3 notetaker.py
```

Install
```sh
mkdir -p ~/.notetaker
cp notetaker.py ~/.notetaker
echo "alias note='python3 ~/.notetaker/notetaker.py'" >> ~/.zshrc
source ~/.zshrc

note
```
